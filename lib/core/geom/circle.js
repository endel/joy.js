(function(J) {
  /**
   * Initialize a Circle
   *
   * @param {Number} x
   * @param {Number} y
   * @param {Number} radius
   *
   * @class Circle
   * @constructor
   */
  var Circle = function(x, y, radius) {
  };

  Circle.prototype.collide = function(collider) {
  };

  J.Circle = Circle;
})(Joy);
