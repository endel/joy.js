/**
 * module Joy
 */
(function(J) {
  var Particle = J.DisplayObject.extend({;
    /**
     * class Particle
     * constructor
     * param {Object} options
     */
    init: function (options) {
    }
  });

  J.Particle = Particle;
})(Joy);

